<h1>Unsere Projekte in <%= @city.title %></h1>

<div class="row">
<div class="col-md-3">
	<h3>Kategorien:</h3>
	<%= link_to "Alle", city_path(@city) %>
	
	<%= render :partial => 'categories', :locals => {:categories => @categories} %>
	
	<h4>Verantwortliche Nutzer:</h4>
	<ul class="list-group">
		<% @city.users.each do |user| %>
			
			<% if user_signed_in? %>
				<a class="list-group-item clearfix" href="<%=user_url(user) %>">
					<%=image_tag user.avatar.url(:thumb), :class => "pull-left" %>
					&nbsp;<%=user.name %>
				</a>
			<% else %>
				<li class="list-group-item">
					<%= user.name %>
				</li>
			<% end %>
		<% end %>
	</ul>
	<% if user_signed_in? %>
	  <% if current_user.try(:admin?) || @city.users.include?(current_user) %>
<h4>Nutzer einladen</h4>
<p><%=link_to request.base_url+invite_city_path(@city), invite_city_path(@city)%></p>
<p>Sende diesen Link an einen Freund. Sobald er/sie sich registriert hat, kann er/sie eigen Projekte in <%= @city.title %> anlegen.</p>	<% end %><% end %>
</div>
<div class="col-md-9">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Category</th>
	<% if user_signed_in? %>
	  <% if current_user.try(:admin?) %>
      <th colspan="2"></th>
<% end %><% end %>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr <% if project.urgent == true %> class="danger"<% end %>>
        <td><%= link_to project.title, project %></td>
        <td><%= link_to project.category.title, project %></td>
		<% if user_signed_in? %>
		  <% if current_user.try(:admin?) %>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		<% end %><% end %>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
	<% if user_signed_in? %>
	  <% if current_user.try(:admin?) || @city.users.include?(@current_user) %>
<%= link_to 'New Project', new_project_path(city_id: @city.id)  %>
<% end %><% end %>

</div>
</div>


<% if user_signed_in? %>
  <% if current_user.try(:admin?) %>
<%= link_to 'Edit', edit_city_path(@city) %> |
<% end %><% end %>
<%= link_to 'Back', cities_path %>
