<p id="notice"><%= notice %></p>
<h1>Unsere Projekte in </h1>

<div class="row">
<div class="col-md-3">
	<h3>Kategorien:</h3>
	<%= render :partial => 'categories', :locals => {:categories => @categories} %>
	
	
</div>
<div class="col-md-9">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Category</th>
      <th>Urgent</th>
	<% if user_signed_in? %>
	  <% if current_user.try(:admin?) %>
      <th colspan="2"></th>
<% end %><% end %>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= link_to project.title, project %></td>
        <td><%= link_to project.category.title, project %></td>
        <td><%= link_to project.urgent, project %></td>
		<% if user_signed_in? %>
		  <% if current_user.try(:admin?) %>
        <td><%= link_to 'Edit', edit_project_path(project) %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		<% end %><% end %>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
	<% if user_signed_in? %>
	  <% if current_user.try(:admin?) %>
<%= link_to 'New Project', new_project_path(city_id: @city.id)  %>
<% end %><% end %>

</div>
</div>


<% if user_signed_in? %>
  <% if current_user.try(:admin?) %>
<%= link_to 'Edit', edit_city_path(@city) %> |
<% end %><% end %>
<%= link_to 'Back', cities_path %>
